<UserControl
    x:Class="DivePlannerMk3.Views.UserControls.DivePlan.PlanGasManagementSetup"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <StackPanel>
        <StackPanel Background="#FF5D99C6">
            <TextBlock
                Margin="4"
                FontSize="16"
                FontWeight="DemiBold"
                Text="Gas Management" />
        </StackPanel>
        <StackPanel IsEnabled="{Binding DivePlan.GasManagement.UiEnabled}">
            <StackPanel Margin="4,0" Background="#FFC3FDFF">
                <TextBlock Margin="4" Text="Cylinder Volume (L):" />
                <TextBox Margin="4" Text="{Binding DivePlan.GasManagement.GasManagementModel.CylinderVolume}" />
                <TextBlock Margin="4" Text="Cylinder Pressure (Bar):" />
                <TextBox Margin="4" Text="{Binding DivePlan.GasManagement.GasManagementModel.CylinderPressure}" />
                <TextBlock Margin="4" Text="SAC Rate (L/min):" />
                <TextBox Margin="4" Text="{Binding DivePlan.GasManagement.GasManagementModel.SacRate}" />
            </StackPanel>
        </StackPanel>
    </StackPanel>

</UserControl>
